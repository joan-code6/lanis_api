[Unit]
Description=Schulportal Hessen API (lanis_api)
After=network.target

[Service]
Type=simple
WorkingDirectory={{PROJECT_DIR}}
EnvironmentFile={{PROJECT_DIR}}/.env
ExecStart={{PYTHON_BIN}} -m api
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
